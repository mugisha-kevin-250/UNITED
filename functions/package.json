{
  "name": "united-functions",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build needed'",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": "18"
  }
}
